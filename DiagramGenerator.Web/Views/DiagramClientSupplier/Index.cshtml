@model DiagramSuppliersPageViewModel

<div class="submit-button">
    <input type="button" class="btn btn-info" value="Back" onclick="window.location= '@Url.Action( "Criteria", "DiagramOutput",  new { diagramId = Model.DiagramId })'" />
</div>

<h4>Suppliers</h4>

@using (Html.BeginForm("SaveSuppliers", "DiagramClientSupplier", FormMethod.Post))
{
    @Html.HiddenFor(x => x.DiagramId)
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col"></th>
                <th scope="col">Lp</th>
                <th scope="col">Name</th>
                <th scope="col">Descritpion</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Suppliers != null)
            {
                @for (var i = 0; i < Model.Suppliers.Count; i++)
                {
                    <tr>
                        @Html.HiddenFor(model => model.Suppliers[i].Id)
                        @Html.HiddenFor(model => model.Suppliers[i].Name)
                        @Html.HiddenFor(model => model.Suppliers[i].Description)
                        @Html.HiddenFor(model => model.Suppliers[i].DiagramId)
                        @Html.HiddenFor(model => model.Suppliers[i].UserEmail)
                        @Html.HiddenFor(model => model.Suppliers[i].Lp)
                        <td>@Html.EditorFor(model => model.Suppliers[i].IsSelected)</td>
                        <td>@Html.DisplayFor(model => model.Suppliers[i].Lp)</td>
                        <td>@Html.DisplayFor(model => model.Suppliers[i].Name)</td>
                        <td>@Html.DisplayFor(model => model.Suppliers[i].Description)</td>
                    </tr>
                }
            }
        </tbody>
    </table>

    <div class="submit-button text-center">
        <input type="submit" class="btn btn-info" value="Submit" />
    </div>
    <div class="text-success text-center">@ViewBag.UserMessage</div>
}

<div class="submit-button text-right">
    <input type="button" class="btn btn-info" value="Next" onclick="window.location= '@Url.Action( "Clients", "DiagramClientSupplier", new {  diagramId = Model.DiagramId } )'" />
</div>


