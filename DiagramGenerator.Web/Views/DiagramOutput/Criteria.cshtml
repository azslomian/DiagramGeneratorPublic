@model DiagramCriteriumPageViewModel

<div class="submit-button">
    <input type="button" class="btn btn-info" value="Back" onclick="window.location= '@Url.Action( "Index", "DiagramOutput",  new { diagramId = Model.DiagramId })'" />
</div>

<h4>Criteria</h4>

@using (Html.BeginForm("SaveCriteria", "DiagramOutput", FormMethod.Post))
{
    @Html.HiddenFor(x => x.DiagramId)
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col"></th>
                <th scope="col">Lp</th>
                <th scope="col">Name</th>
                <th scope="col">Descritpion</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Criteria != null)
            {
                @for (var i = 0; i < Model.Criteria.Count; i++)
                {
                    <tr>
                        @Html.HiddenFor(model => model.Criteria[i].Id)
                        @Html.HiddenFor(model => model.Criteria[i].Name)
                        @Html.HiddenFor(model => model.Criteria[i].Description)
                        @Html.HiddenFor(model => model.Criteria[i].DiagramId)
                        @Html.HiddenFor(model => model.Criteria[i].UserEmail)
                        @Html.HiddenFor(model => model.Criteria[i].Lp)
                        <td>@Html.EditorFor(model => model.Criteria[i].IsSelected)</td>
                        <td>@Html.DisplayFor(model => model.Criteria[i].Lp)</td>
                        <td>@Html.DisplayFor(model => model.Criteria[i].Name)</td>
                        <td>@Html.DisplayFor(model => model.Criteria[i].Description)</td>
                    </tr>
                }
            }
        </tbody>
    </table>

    <div class="submit-button text-center">
        <input type="submit" class="btn btn-info" value="Submit" />
    </div>
    <div class="text-success text-center">@ViewBag.UserMessage</div>
}


<div class="submit-button text-right">
    <input type="button" class="btn btn-info" value="Next" onclick="window.location= '@Url.Action( "Index", "DiagramClientSupplier", new {  diagramId = Model.DiagramId } )'" />
</div>

